#include <stdio.h>
int main() {
    int a = 10;
    printf("The size of the variable a is: %lu bytes\n", sizeof(a));
    printf("The size of the int data type is: %lu bytes\n", sizeof(int));
    return 0;
}